import React from 'react';
import {Button, Text, View} from 'react-native';

const MealDetail = ({navigator}) => {
  return (
    <View>
      <Text>Meal Detail!</Text>
    </View>
  );
};

export default MealDetail;
