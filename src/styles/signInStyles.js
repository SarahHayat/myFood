import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  button: {
    padding: 20,
    borderWidth: 1,
    borderRadius: 30,
  },
  touchable: {
    margin: 20,
    alignSelf: 'center',
  },
});

export {styles};
