import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  ingredientView: {
    flex: 1,
    alignItems: 'center',
  },
  ingredientImage: {
    width: 90,
    height: 90,
  },
});

export default {styles};
